import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.samarth.transformermonitor',
  appName: 'Transformer Monitor',
  webDir: 'dist'
};

export default config;
